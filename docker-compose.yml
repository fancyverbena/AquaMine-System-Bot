version: '3.8'
services:
  bot:
    build: .
    container_name: aquamine-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
      - ./data:/app/data
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}